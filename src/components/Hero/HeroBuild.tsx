import React from 'react';
import {} from './Styled';

const HeroConstructor = () => {
  return <></>;
};

HeroConstructor.defaultProps = {};

const HeroBuild = React.memo(HeroConstructor);

export default HeroBuild;
export { HeroBuild };
