import React from 'react';
import { HeroBuild } from '@components';
import {} from './Styled';

const HeroMain = () => {
  return <></>;
};

HeroMain.defaultProps = {};

const HeroDefault = React.memo(HeroMain);

export default HeroDefault;
export { HeroDefault };
